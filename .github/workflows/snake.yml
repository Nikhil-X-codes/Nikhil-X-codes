# This workflow is no longer used.
# The contribution snake has been replaced by assets/name-snake.svg
# which is a custom "NIKHIL" dot-matrix snake animation generated by
# generate_name_snake.py — no GitHub Actions needed.
#
# Original workflow preserved below for reference:
#
# name: Generate Snake Animation
# on:
#   schedule:
#     - cron: "0 0 * * *"
#   workflow_dispatch:
# jobs:
#   generate:
#     permissions:
#       contents: write
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Generate Snake SVG
#         uses: Platane/snk/svg-only@v3
#         with:
#           github_user_name: ${{ github.repository_owner }}
#           outputs: |
#             dist/github-snake.svg
#             dist/github-snake-dark.svg?palette=github-dark
#       - name: Push to output branch
#         uses: crazy-max/ghaction-github-pages@v3
#         with:
#           target_branch: output
#           build_dir: dist
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
